# Cloudflare API token "homelab-terraform" (permissions: README.md).
# Copy to cloudflare-api-token.sops.yaml, fill in, then encrypt in place:
#   sops -e -i terraform/cloudflare/secrets/cloudflare-api-token.sops.yaml
CLOUDFLARE_API_TOKEN: "<token>"
