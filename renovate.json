{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "timezone": "America/Detroit",
  "kubernetes": {
    "managerFilePatterns": ["/^argocd/apps/.+\\.ya?ml$/"]
  },
  "argocd": {
    "managerFilePatterns": ["/^argocd/apps/.+/application\\.ya?ml$/"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Authelia's own image tag is pinned in valuesObject, separately from the chart's targetRevision (which the argocd manager already tracks) — neither built-in manager sees a nested image.tag override, so this catches it via an explicit annotation comment.",
      "managerFilePatterns": ["/^argocd/apps/authelia/application\\.yaml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)\\s+tag: '(?<currentValue>.*?)'"
      ]
    }
  ]
}
