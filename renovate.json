{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "timezone": "America/Detroit",
  "kubernetes": {
    "managerFilePatterns": [
      "/^argocd/apps/.+\\.ya?ml$/",
      "/^manifests/.+\\.ya?ml$/"
    ]
  },
  "argocd": {
    "managerFilePatterns": ["/^argocd/apps/.+/application\\.ya?ml$/"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Image tags pinned in an Application's valuesObject (Authelia, democratic-csi), separately from the chart's targetRevision (which the argocd manager already tracks) — neither built-in manager sees a nested image.tag override, so this catches it via an explicit annotation comment.",
      "managerFilePatterns": [
        "/^argocd/apps/authelia/application\\.yaml$/",
        "/^argocd/apps/democratic-csi/application\\.yaml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)\\s+tag: '(?<currentValue>.*?)'"
      ]
    },
    {
      "customType": "regex",
      "description": "ArgoCD's own chart version is bumped by hand via the `helm install`/`helm upgrade` command documented in argocd/README.md (see 'Upgrading ArgoCD itself') — it isn't a Kubernetes/ArgoCD Application manifest the built-in managers scan, so this catches it via an explicit annotation comment, same pattern as the Authelia one above. A PR here only updates the doc; the actual `helm upgrade` against the cluster is still a manual step by design.",
      "managerFilePatterns": ["/^argocd/README\\.md$/"],
      "matchStrings": [
        "<!-- renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?) registryUrl=(?<registryUrl>\\S*?) -->[\\s\\S]*?--version (?<currentValue>[0-9.]+)"
      ]
    }
  ]
}
